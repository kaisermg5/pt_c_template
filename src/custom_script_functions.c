
#include <types.h>
#include <script.h>

// I was going to replace the command Nop1, but then realized there was a bug so... Commented it is

/*ScriptCommand ScriptSpecials[];

u32 CustomScrCmd_CallSpecial(struct ScriptHandler * handler) {

	return ScriptSpecials[0](handler);
}

u32 ScriptSpecial_test(struct ScriptHandler * handler) {

	return 0;
}


ScriptCommand ScriptSpecials[] = {
	ScriptSpecial_test
};

*/